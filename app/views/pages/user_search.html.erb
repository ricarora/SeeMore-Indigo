<h2>User Search</h2>


<%= form_tag user_search_path do %>
  <%= select_tag "provider", options_for_select([ "Twitter", "Vimeo" ]) %>
  <%= text_field_tag :search, params[:search], :placeholder => "Search for user" %>
  <%= submit_tag "Search" %>
<% end %>
